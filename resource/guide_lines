
#1.		Faire un Makefile -- Done.

#2. 	Gestion d'erreur -- Done.

		2.1	Parser les options [-options] -- Done.
		2.2 Parser les files ou directory -- Done.

#3. 	Implementation des fonctions d'affichages [current].

#4. 	Implementation des fonctions de tries [not current].

##.		Affichage a implementer
		- if (ac == 1)
	  	juste faire un opendir du repertoire courant,
	  	trier lexico tout ce qu'il y a dedans et quitter proprement.

###.	Tools :

	- /|\ "drwxrwxr-x+" ou "drwxr-xr-x@"
	- Le + signifie qu'il y a des ACL sur le fichier/répertoire, c'est a dire une gestion des droits plus complètes
	  que le simple "user/groupe/autres".
	- Le @ quant à lui signifie qu'il y a des attributs étendus sur le fichier / dossier.
	  Pour celle là, test la commande système xattr depuis ton shell...

	typedef struct 			s_fmode
	{
		t_bool				is_file;
		t_bool				is_dir;
		t_bool				is_chr;
		t_bool				is_blk;
		t_bool				is_fifo;
		t_bool				is_lnk;
		t_bool				is_sock;

(Faire pointer le dernier maillon d'une de mes listes chainée vers la suivante)
(Retrier la liste pointant vers la suivante).

ft_putendl("......... DEGUG ZONE .........");
ft_putstr("Option d'affichage : ");
ft_putchar(e->flg->aff);
ft_putchar('\n');
ft_putstr("option -a : ");
ft_putnbr(e->flg->all);
ft_putchar('\n');
ft_putstr("option -R : ");
ft_putnbr(e->flg->rec);
ft_putchar('\n');
ft_putstr("option -r : ");
ft_putnbr(e->flg->rev);
ft_putchar('\n');
ft_putstr("option -t : ");
ft_putnbr(e->flg->t);
ft_putchar('\n');


l'option -S et -h -i -G sont tres simple -n egalement ;)
Ne pas oublier print list error de le faire en fd 2

**********(./ft_ls -l) ***********

[
	Si dans les arguments on a minimum 1 dossier valide alors les fichiers sont print avant.
		-- Si quelque chose a ete print une erreur ou un file ou un dossier alors (ce qui fait mini 2 en somme):
			path:
			total xx
			...
]
-- Si on a que des fichiers on ne met pas le total
-- Sinon si on nous passe uniquement des options donc ni fichier ou dossier valide on traite repertoire courant.
